<?xml version="1.0" encoding="UTF-8"?>
<Container version="2">
  <Name>binance-to-influxdb</Name>
  <Repository>nixoninnes/binance-to-influxdb:latest</Repository>
  <Registry>https://registry.hub.docker.com/r/nixoninnes/binance-to-influxdb</Registry>
  <Network>bridge</Network>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://forums.unraid.net/topic/105293-support-nixoninnes-unraid-builds/</Support>
  <Overview>Binance to Influx is a container designed to be run on an [Unraid](https://www.unraid.net/) server via Docker. The purpose of the container is to simplify the ingestion of cryptocurrency market data, provided by the [Binance API](https://binance.com/), into your own [Influx database](https://www.influxdata.com/).  &#13;
The container collects the candles (klines) for each of the configured crypto symbols (e.g. BTCUSDT). </Overview>
  <WebUI></WebUI>
  <TemplateURL>https://raw.githubusercontent.com/NixonInnes/unraid-builds-xml/main/binance-to-influxdb.xml</TemplateURL>
  <Icon></Icon>
  <PostArgs></PostArgs>
  <CPUset></CPUset>
  <Networking>
    <Mode>bridge</Mode>
    <Publish></Publish>
  </Networking>
  <Data>
    <Volume>
      <HostDir>/mnt/user/appdata/binance-to-influxdb</HostDir>
      <ContainerDir>/config</ContainerDir>
      <Mode>rw</Mode>
    </Volume>
  </Data>
  <Environment>
    <Variable>
      <Value></Value>
      <Name>INFLUXDB_TOKEN</Name>
      <Mode></Mode>
    </Variable>
  </Environment>
  <Config Name="Config" Target="/config" Default="/mnt/user/appdata/binance-ingester" Mode="rw" Description="Container Path: /config" Type="Path" Display="always" Required="false" Mask="false">/mnt/user/appdata/binance-to-influxdb</Config>
  <Config Name="InfluxDB Token" Target="INFLUXDB_TOKEN" Default="" Mode="" Description="Influx database access token" Type="Variable" Display="always" Required="true" Mask="false"></Config>
  <Date>1617668200</Date>
  <Description>Binance to Influx is a container designed to be run on an (https://www.unraid.net/) server via Docker. The purpose of the container is to simplify the ingestion of cryptocurrency market data, provided by the (https://binance.com/), into your own (https://www.influxdata.com/).  &#13;
The container collects the candles (klines) for each of the configured crypto symbols (e.g. BTCUSDT).</Description>
  <templatePath>/tmp/GitHub/AppFeed/templates/NixonInnesRepository/binance-to-influxdb.xml</templatePath>
</Container>
